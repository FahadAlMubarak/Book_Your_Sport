<div class="home-container">
  <div class="video-container">
    <video class="darken-video" playsinline autoplay muted loop poster="<%= asset_path('tennis-background.jpg') %>" id="bgvid">
      <source src="<%= asset_path('tennis.mp4') %>" type="video/mp4">
    </video>

    <div class="hero-content">
      <h1 class="text-white">Game On...</h1>
      <h5 class="special text-white pb-3">Book Your Sport, Anytime, Anywhere!</h5>
        <a class="mt-2 w-50 btn-secondary special" href="<%= venues_path %>">See all our venues</a>
    </div>
  </div>

  <div class="about-us-container">
  <h2 class="pb-4">ABOUT US</h2>
    <div class="about-us-card">
      <h4 class="pb-3">Flexibility</h4>
      <div class="border-bottom-light"></div>
    <p class="pt-3"> Our app is designed to provide you with the flexibility to play anywhere, anytime. Whether it's soccer, cricket, tennis, or any other sport, you can easily find and book the perfect venue.</p>
  </div>
      <div class="about-us-card">
      <h4 class="pb-3">Compare</h4>
      <div class="border-bottom-light"></div>
    <p class="pt-3">Our app lets you compare different options based on price, location, and availability, so you can find the perfect spot for your next game.</p>
  </div>
      <div class="about-us-card">
      <h4 class="pb-3">Sports owners</h4>
      <div class="border-bottom-light"></div>
    <p class="pt-3"> If you want to list your own venue and make it easier for people to enjoy the sports you have on offer, then this app makes it very convenient and seamless for you! </p>
  </div>
  </div>

  <div class="top-list-venues mt-5 pt-3">
  <h3 class="pb-3 text-black">Some of our venues</h1>
    <div id="carouselExample" class="carousel slide" data-bs-ride="carousel">
      <div class="carousel-inner">
        <!-- Replace static carousel items with dynamic content -->
        <% @venues.each_with_index do |venue, index| %>
          <div class="carousel-item <%= 'active' if index == 0 %>">
            <div class="card-trip">
              <% if venue.images.attached? %>
                <%= cl_image_tag venue.images.first.key %>
              <% else %>
                <%= image_tag("backdrop.jpg") %>
              <% end %>
              <div class="card-trip-infos">
                <div style="flex-grow: 1;">
                  <h3><%= venue.name %></h3>
                  <h5 class="special"><%= venue.borough %></h5>
                  <p class="pt-2"><%= venue.description %></p>
                  <p><%= venue.sports %></p>
                  <p><%= venue.opening_time.strftime("%I:%M %p") %> - <%= venue.closing_time.strftime("%I:%M %p") %></p>
                </div>
                <div style="width: 105px;">
                  <% if venue.logo.attached? %>
                    <%= cl_image_tag venue.logo.key, class: "logo" %>
                  <% else %>
                    <%= image_tag("venue_default_avatar.png", class: "logo") %>
                  <% end %>
                </div>
              </div>
              <div class="btn-area w-100">
                <a href="<%= venue_path(venue) %>" class="w-100 btn-green">See More</a>
              </div>
            </div>
          </div>
        <% end %>
      </div>
      <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
      </button>
    </div>
  </div>
</div>
