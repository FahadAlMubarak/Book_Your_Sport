<% if bookings.any? %>
  <%# <% bookings.order(created_at: :desc).each do |booking| %>
  <% bookings.each do |booking| %>
    <%# <% booking = booking[0] if booking.length >1 %>
    <div class='my-3 border-bottom border-grey booking-wrapper'>
      <div class="booking-container d-flex flex-row mb-3 ">
        <%# <div class="row w-100">
          <div class="col-2"> %>
            <%# <div class="date-group"> %>

            <%# </div> %>
          <%# </div> %>
          <%# <div class="col-7"> %>
            <div class="time-location-group" style='flex-grow: 1;'>
              <p><i class="fa-solid fa-clock"></i> <%= booking.start_time.strftime("%d-%m-%-y") %> <%= booking.start_time.strftime("%H:%M") %> - <strong><%= booking.end_time.strftime("%H:%M") %></strong></p>
              <p><i class="fa-solid fa-location-dot" style='padding-left: 2px; padding-right: 2px;'></i> <strong><%= booking.venue.name %></strong>, <%= booking.venue_address %></p>
            </div>
          <%# </div> %>
          <%# <div class="col-4">
          </div> %>
          <%# <div class="col-3"> %>
            <div class="renter-details-group">
              <% venue = booking.venue %>
              <% if booking.venue.logo.attached? %>
                <%= image_tag booking.venue.logo, class: "logo-inline" %>
              <% else %>
                <%= image_tag "venue_default_avatar.png", class: "logo-inline" %>
              <% end %>
            </div>
          <%# </div>
        </div> %>
      </div>
      <% if booking.start_time < DateTime.now && booking.review.nil? %>
        <%= link_to new_booking_review_path(booking), class: ' btn-small w-30' do %>
          Review
        <% end %>
      <% end %>
    </div>
  <% end %>
<% else %>
<%# <div class="booking-container-past mt-3"> %>
  <p>No bookings! You can book <%= link_to "here", venues_path %></p>
  <%# </div> %>
<% end %>
